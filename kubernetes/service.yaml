apiVersion: v1
kind: Service
metadata:
  name: smart-deployment-guard-service
spec:
  type: NodePort
  selector:
    app: smart-deployment-guard
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30080